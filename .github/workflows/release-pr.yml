name: Release PR

on:
  push:
    branches: [master]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-pr:
    uses: DragonAddons/wow-workflows/.github/workflows/release-pr.yml@main
    secrets: inherit
