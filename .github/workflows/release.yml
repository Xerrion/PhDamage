name: Release

on:
  push:
    branches: [master]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: DragonAddons/wow-workflows/.github/workflows/release.yml@main
    secrets: inherit
